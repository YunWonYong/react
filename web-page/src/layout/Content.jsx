import React from "react";

const Content = ({ children }) => {
  return (
    <section
      style={{
        paddingBottom: "4.7em",
      }}
    >
      {children}
    </section>
  );
};

export default Content;
